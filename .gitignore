# --- Environnements Virtuels ---
# Il ne faut jamais envoyer tout le dossier de l'environnement sur Git
mon_env/
venv/
env/

# --- Python ---
# Fichiers compilés par Python (inutiles sur Git)
__pycache__/
*.pyc

# --- Configuration & Sécurité ---
# TRÈS IMPORTANT : Contient tes variables d'environnement (mots de passe, clés)
.env

# Si 'config.py' contient des mots de passe en dur, décommente la ligne suivante :
# config.py

# --- Fichiers de données (Excel/CSV) ---
# On évite généralement de versionner les données brutes ou de sortie
*.xlsx
*.xls
*.csv

# Si tu veux quand même garder un fichier Excel spécifique (ex: un modèle vide),
# tu peux ajouter une exception avec un point d'exclamation :
# !mon_template.xlsx

# --- Fichiers générés ou personnels ---
Mon_cv.pdf